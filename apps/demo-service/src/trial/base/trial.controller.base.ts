/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { TrialService } from "../trial.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { TrialCreateInput } from "./TrialCreateInput";
import { TrialWhereInput } from "./TrialWhereInput";
import { TrialWhereUniqueInput } from "./TrialWhereUniqueInput";
import { TrialFindManyArgs } from "./TrialFindManyArgs";
import { TrialUpdateInput } from "./TrialUpdateInput";
import { Trial } from "./Trial";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class TrialControllerBase {
  constructor(
    protected readonly service: TrialService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Trial })
  @nestAccessControl.UseRoles({
    resource: "Trial",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(@common.Body() data: TrialCreateInput): Promise<Trial> {
    return await this.service.create({
      data: {
        ...data,

        experiment: data.experiment
          ? {
              connect: data.experiment,
            }
          : undefined,
      },
      select: {
        createdAt: true,

        experiment: {
          select: {
            id: true,
          },
        },

        id: true,
        trialBody: true,
        trialIndex: true,
        trialType: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Trial] })
  @ApiNestedQuery(TrialFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Trial",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<Trial[]> {
    const args = plainToClass(TrialFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,

        experiment: {
          select: {
            id: true,
          },
        },

        id: true,
        trialBody: true,
        trialIndex: true,
        trialType: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Trial })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Trial",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: TrialWhereUniqueInput
  ): Promise<Trial | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,

        experiment: {
          select: {
            id: true,
          },
        },

        id: true,
        trialBody: true,
        trialIndex: true,
        trialType: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Trial })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Trial",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: TrialWhereUniqueInput,
    @common.Body() data: TrialUpdateInput
  ): Promise<Trial | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          experiment: data.experiment
            ? {
                connect: data.experiment,
              }
            : undefined,
        },
        select: {
          createdAt: true,

          experiment: {
            select: {
              id: true,
            },
          },

          id: true,
          trialBody: true,
          trialIndex: true,
          trialType: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Trial })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Trial",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: TrialWhereUniqueInput
  ): Promise<Trial | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,

          experiment: {
            select: {
              id: true,
            },
          },

          id: true,
          trialBody: true,
          trialIndex: true,
          trialType: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
